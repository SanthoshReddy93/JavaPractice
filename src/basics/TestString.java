package basics;
/*
 * String is a sequence of characters and it is a class in java
 *   but considered as a literal because of its unique behavior
 *   
 *    different ways to create a string 
 *    Strings1 = "hello";
 *    Strings2 = new String("hello"); 
 * String Methods :-
 *   1.Starts with [s1.startswith("H")]
 *   2.ends with 
 *   3.contains :- particular letter or word not lower case test verification
 *   4.equals
 *   5.equalsIgnoreCase :- either capital or small it is execute
 *   6.trim :- [s1.trim()] remove extra wide spaces remover
 *   7.toLowerCase
 *   8.toUpperCase
 *   9.toCharArray
 *   10.length
 *   11.indexOf== first and lastIndexOf
 *   12.subString == 
 *   13.split = (Arrays.toString(s1.split(""))
 *   14.
 *   15.valueOf = for conversion to integer and number 
 *   16.isEmpty = 
 *   17.isBlank =
 */
public class TestString {

	public static void main(java.lang.String[] args) {
		String s1 ="Hello world";
				String s2 ="Hello";
				String s3 ="world";
	}

}
